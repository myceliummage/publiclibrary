// define application namespace and sub-namespaces using "namespace objects". includes root object 
// (pl namespace) and one sub-namespace each for the model, view, and controller
var pl = { m:{}, v:{}, c:{} };